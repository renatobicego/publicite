import { ProductionTicketServiceInterface } from '../../domain/service/production-ticket.service.interface';

/** Adapter entre los resolvers de tickets y su service. */
export type ProductionTicketAdapterInterface = ProductionTicketServiceInterface;
