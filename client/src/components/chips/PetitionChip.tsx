import { Chip } from "@nextui-org/react";

const PetitionChip = () => {
  return (
    <Chip className="border-petition text-xs" variant="bordered" size="sm">
      Necesidad
    </Chip>
  );
};

export default PetitionChip;
