import { Chip } from "@nextui-org/react";

const ServiceChip = () => {
  return (
    <Chip className="text-service border-service" size="sm" variant="bordered">
      Servicio
    </Chip>
  );
};

export default ServiceChip;
